---
phase: 01-end-to-end-workbench-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/core/api/api-client.ts
  - frontend/src/core/config/index.ts
  - frontend/src/core/config/runtime-base-url.ts
  - frontend/src/core/threads/hooks.ts
  - frontend/src/core/threads/use-thread-stream.state.ts
autonomous: true
requirements:
  - FLOW-01
user_setup: []
must_haves:
  truths:
    - "Submitting the first prompt from /workspace/submarine/new no longer throws Failed to construct 'URL': Invalid URL in the browser."
    - "The LangGraph client always receives an absolute apiUrl before thread creation or stream submit is attempted."
    - "New-thread bootstrap creates the thread before submit and binds that thread id to the active stream target."
  artifacts:
    - path: "frontend/src/core/api/api-client.ts"
      provides: "LangGraph client construction with validated apiUrl"
      contains: "getLangGraphBaseURL"
    - path: "frontend/src/core/threads/hooks.ts"
      provides: "Transactional sendMessage bootstrap for new threads"
      contains: "apiClient.threads.create"
    - path: "frontend/src/core/threads/use-thread-stream.state.ts"
      provides: "Explicit send-state rules for /new bootstrap"
      contains: "shouldCreateThreadBeforeSubmit"
  key_links:
    - from: "frontend/src/core/api/api-client.ts"
      to: "frontend/src/core/config/runtime-base-url.ts"
      via: "getLangGraphBaseURL resolution"
      pattern: "getLangGraphBaseURL"
    - from: "frontend/src/core/threads/hooks.ts"
      to: "frontend/src/core/api/api-client.ts"
      via: "apiClient.threads.create and stream submit"
      pattern: "apiClient\\.threads\\.create"
    - from: "frontend/src/core/threads/hooks.ts"
      to: "frontend/src/core/threads/use-thread-stream.state.ts"
      via: "deriveThreadStreamSendState"
      pattern: "deriveThreadStreamSendState"
---

# Phase 1 Plan 01: Repair New-Thread LangGraph Bootstrap

<objective>
Restore the first-submit transport path for `/workspace/submarine/new`.

Purpose: FLOW-01 is blocked before any backend request leaves the browser, so Phase 1 cannot advance until thread creation and stream submit are wired correctly.
Output: A valid LangGraph client construction path and a deterministic new-thread create/bind/submit flow.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-end-to-end-workbench-bootstrap/01-CONTEXT.md
@.planning/phases/01-end-to-end-workbench-bootstrap/01-RESEARCH.md
@.planning/phases/01-end-to-end-workbench-bootstrap/01-VALIDATION.md
@frontend/src/core/api/api-client.ts
@frontend/src/core/config/index.ts
@frontend/src/core/config/runtime-base-url.ts
@frontend/src/core/threads/hooks.ts
@frontend/src/core/threads/use-thread-stream.state.ts
@frontend/src/app/workspace/submarine/[thread_id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden LangGraph client construction against invalid or stale base URLs</name>
  <files>frontend/src/core/api/api-client.ts, frontend/src/core/config/index.ts, frontend/src/core/config/runtime-base-url.ts</files>
  <read_first>frontend/src/core/api/api-client.ts, frontend/src/core/config/index.ts, frontend/src/core/config/runtime-base-url.ts, frontend/src/core/config/runtime-base-url.test.ts</read_first>
  <action>Refactor `createCompatibleClient(isMock)` so it resolves `const apiUrl = getLangGraphBaseURL(isMock)` and validates that value before constructing `new LangGraphClient({ apiUrl })`. If the resolved value is empty, relative when it should be absolute, or otherwise rejected by `new URL(...)`, throw `new Error("LangGraph base URL is empty or invalid")`. Replace the single `_singleton ??=` client cache with a cache keyed by resolved `apiUrl` and mock mode so a stale client created for a different mode or endpoint is never reused for the submarine workbench bootstrap. Keep the `sanitizeRunStreamOptions()` wrappers on `runs.stream` and `runs.joinStream` intact.</action>
  <verify>corepack pnpm typecheck</verify>
  <acceptance_criteria>
    - frontend/src/core/api/api-client.ts contains `const apiUrl = getLangGraphBaseURL(isMock)`
    - frontend/src/core/api/api-client.ts contains `LangGraph base URL is empty or invalid`
    - frontend/src/core/api/api-client.ts no longer relies on a single `_singleton ??=` path that ignores resolved URL changes
  </acceptance_criteria>
  <done>LangGraph client creation is keyed by a validated API URL and cannot silently reuse a mismatched bootstrap client.</done>
</task>

<task type="auto">
  <name>Task 2: Rebind new-thread submit flow to the created thread before upload and stream submit</name>
  <files>frontend/src/core/threads/hooks.ts, frontend/src/core/threads/use-thread-stream.state.ts</files>
  <read_first>frontend/src/core/threads/hooks.ts, frontend/src/core/threads/use-thread-stream.state.ts, frontend/src/components/workspace/chats/use-thread-chat.ts, frontend/src/app/workspace/submarine/[thread_id]/page.tsx</read_first>
  <action>Keep `sendMessage(threadId, message, extraContext)` transactional for `/new` routes: call `apiClient.threads.create({ ifExists: "do_nothing", threadId })`, bind `threadIdRef` and `onStreamThreadId` immediately, and wait for `threadStreamRef.current` to move off the stale `thread` instance before calling `.submit()`. Use the same created/generated `threadId` for `uploadFiles(threadId, files)`, submit options `threadId`, and `context.thread_id`. If the rebound stream is still unavailable after the retry loop, throw an explicit bootstrap error such as `Thread bootstrap stream did not rebind` instead of submitting against a stale target. Preserve `notifyKnownThreadStart()` ordering so route replacement can still happen only after the thread is known.</action>
  <verify>corepack pnpm typecheck</verify>
  <acceptance_criteria>
    - frontend/src/core/threads/hooks.ts still contains `apiClient.threads.create` before first submit on new threads
    - frontend/src/core/threads/hooks.ts uses the same `threadId` for `uploadFiles(`, submit `threadId`, and `context.thread_id`
    - frontend/src/core/threads/hooks.ts contains `Thread bootstrap stream did not rebind` or an equivalent explicit bootstrap failure
  </acceptance_criteria>
  <done>The first submarine prompt cannot race ahead of thread creation or stream rebinding anymore.</done>
</task>

</tasks>

<verification>
- [ ] `corepack pnpm typecheck`
- [ ] Browser first submit from `/workspace/submarine/new` reaches thread creation without `Failed to construct 'URL': Invalid URL`
</verification>

<success_criteria>

- All tasks completed
- LangGraph client construction is based on a validated absolute URL
- New-thread bootstrap creates and binds the thread before attachment upload or message submit
- FLOW-01 is unblocked at the transport/bootstrap layer

</success_criteria>

<output>
After completion, create `.planning/phases/01-end-to-end-workbench-bootstrap/01-01-SUMMARY.md`
</output>
