---
phase: 01-end-to-end-workbench-bootstrap
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - frontend/src/app/workspace/submarine/[thread_id]/page.tsx
  - frontend/src/components/workspace/submarine-pipeline.tsx
  - frontend/src/components/workspace/chats/use-thread-chat.ts
  - frontend/src/components/workspace/chats/use-thread-chat.state.ts
  - frontend/src/core/threads/hooks.ts
  - frontend/src/core/threads/use-thread-stream.state.ts
autonomous: true
requirements:
  - FLOW-02
user_setup: []
must_haves:
  truths:
    - "Uploaded STL and the first prompt remain bound to the created thread after route replacement from /new to /workspace/submarine/{threadId}."
    - "Refreshing the created submarine thread page rehydrates the attachment and conversation context instead of showing a blank bootstrap state."
    - "The submarine workbench preserves its workbench kind and bootstrap metadata across route transition."
  artifacts:
    - path: "frontend/src/app/workspace/submarine/[thread_id]/page.tsx"
      provides: "Created-thread route replacement and hydration"
      contains: "markThreadStarted"
    - path: "frontend/src/components/workspace/submarine-pipeline.tsx"
      provides: "Submarine submit path tied to the route-owned thread id"
      contains: "sendMessage(threadId, message)"
    - path: "frontend/src/components/workspace/chats/use-thread-chat.ts"
      provides: "Route ownership for generated versus started thread ids"
      contains: "markThreadStarted"
  key_links:
    - from: "frontend/src/app/workspace/submarine/[thread_id]/page.tsx"
      to: "frontend/src/core/threads/hooks.ts"
      via: "onStart route replacement"
      pattern: "markThreadStarted"
    - from: "frontend/src/components/workspace/submarine-pipeline.tsx"
      to: "frontend/src/core/threads/hooks.ts"
      via: "sendMessage callback"
      pattern: "sendMessage\\(threadId, message\\)"
    - from: "frontend/src/components/workspace/chats/use-thread-chat.ts"
      to: "frontend/src/components/workspace/chats/use-thread-chat.state.ts"
      via: "deriveThreadChatRouteState and deriveStartedThreadChatState"
      pattern: "deriveStartedThreadChatState"
---

# Phase 1 Plan 02: Preserve Attachment and Route State After Bootstrap

<objective>
Make the created submarine thread durable after the first prompt succeeds.

Purpose: FLOW-02 requires the uploaded STL and first prompt to survive the `/new` to created-thread transition and remain visible after refresh.
Output: Stable route-state ownership plus rehydrated attachment/message state for created submarine threads.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-end-to-end-workbench-bootstrap/01-CONTEXT.md
@.planning/phases/01-end-to-end-workbench-bootstrap/01-RESEARCH.md
@.planning/phases/01-end-to-end-workbench-bootstrap/01-VALIDATION.md
@.planning/phases/01-end-to-end-workbench-bootstrap/01-01-PLAN.md
@frontend/src/app/workspace/submarine/[thread_id]/page.tsx
@frontend/src/components/workspace/submarine-pipeline.tsx
@frontend/src/components/workspace/chats/use-thread-chat.ts
@frontend/src/components/workspace/chats/use-thread-chat.state.ts
@frontend/src/core/threads/hooks.ts
@frontend/src/core/threads/use-thread-stream.state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carry attachment and bootstrap metadata across the /new route swap</name>
  <files>frontend/src/app/workspace/submarine/[thread_id]/page.tsx, frontend/src/components/workspace/submarine-pipeline.tsx, frontend/src/core/threads/hooks.ts</files>
  <read_first>frontend/src/app/workspace/submarine/[thread_id]/page.tsx, frontend/src/components/workspace/submarine-pipeline.tsx, frontend/src/core/threads/hooks.ts, frontend/src/core/threads/use-thread-stream.state.ts</read_first>
  <action>Keep the first prompt plus uploaded STL attached to the created thread when `onStart(createdThreadId)` fires. Preserve `markThreadStarted(createdThreadId)` before `router.replace(nextPath)` and ensure the created thread record carries forward `workspace_kind: "submarine"`, optimistic human message content, and uploaded file metadata until backend thread state arrives. Reuse `deriveThreadsAfterWorkbenchStart()` and query invalidation rather than inventing a second bootstrap store, but do not let route replacement clear the workbench back to an empty state while the first stream is still bootstrapping.</action>
  <verify>`node --test src/components/workspace/chats/use-thread-chat.state.test.ts`</verify>
  <acceptance_criteria>
    - frontend/src/app/workspace/submarine/[thread_id]/page.tsx contains `markThreadStarted(createdThreadId)` before `router.replace(nextPath)`
    - frontend/src/components/workspace/submarine-pipeline.tsx still calls `sendMessage(threadId, message)` using the route-owned thread id
    - frontend/src/core/threads/hooks.ts preserves `workspace_kind: "submarine"` on the started thread record
  </acceptance_criteria>
  <done>Route replacement no longer drops the uploaded STL or first prompt from the created submarine thread.</done>
</task>

<task type="auto">
  <name>Task 2: Rehydrate created submarine threads on refresh and direct re-entry</name>
  <files>frontend/src/components/workspace/chats/use-thread-chat.ts, frontend/src/components/workspace/chats/use-thread-chat.state.ts, frontend/src/app/workspace/submarine/[thread_id]/page.tsx</files>
  <read_first>frontend/src/components/workspace/chats/use-thread-chat.ts, frontend/src/components/workspace/chats/use-thread-chat.state.ts, frontend/src/app/workspace/submarine/[thread_id]/page.tsx, frontend/src/components/workspace/chats/use-thread-chat.state.test.ts</read_first>
  <action>Align `useThreadChat()`, `deriveThreadChatRouteState()`, and the submarine page bootstrap so any non-`new` route uses the route param thread id consistently after refresh. Keep `threadId: isNewThread ? undefined : threadId` behavior only for true `/new` routes, and ensure created-thread refresh paths re-enter the same workbench kind with previously attached message/file state rather than generating a fresh UUID. If query invalidation is needed after `router.replace`, trigger it from the created-thread path so the first refresh reloads the persisted thread state instead of a blank cockpit.</action>
  <verify>`node --test src/components/workspace/chats/use-thread-chat.state.test.ts`</verify>
  <acceptance_criteria>
    - frontend/src/components/workspace/chats/use-thread-chat.state.ts keeps `routeThreadId === "new"` as the only path that generates a UUID
    - frontend/src/components/workspace/chats/use-thread-chat.ts keeps `markThreadStarted` setting `isNewThread: false`
    - frontend/src/app/workspace/submarine/[thread_id]/page.tsx passes `threadId: isNewThread ? undefined : threadId` only for true new-thread routes
  </acceptance_criteria>
  <done>Refreshing or revisiting a created submarine thread preserves the same thread identity and rehydrates its first bootstrap state.</done>
</task>

</tasks>

<verification>
- [ ] `node --test src/components/workspace/chats/use-thread-chat.state.test.ts`
- [ ] Use Chrome DevTools MCP to upload `C:\Users\D0n9\Desktop\suboff_solid.stl` at `http://localhost:3000/workspace/submarine/new`
- [ ] Confirm the URL changes to `/workspace/submarine/{threadId}` and that a page refresh keeps the uploaded STL and first prompt visible
</verification>

<success_criteria>

- All tasks completed
- Route replacement from `/new` preserves attachment and prompt state
- Refresh uses the created thread id instead of generating a new bootstrap state
- FLOW-02 is satisfied with the provided STL browser flow

</success_criteria>

<output>
After completion, create `.planning/phases/01-end-to-end-workbench-bootstrap/01-02-SUMMARY.md`
</output>
