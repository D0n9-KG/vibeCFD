---
phase: 08-skill-studio-lifecycle-and-governance
plan: 03
type: execute
wave: 3
depends_on:
  - "08-01"
  - "08-02"
files_modified:
  - backend/packages/harness/deerflow/domain/submarine/skill_lifecycle.py
  - backend/app/gateway/routers/skills.py
  - backend/packages/harness/deerflow/skills/relationships.py
  - backend/tests/test_skill_lifecycle_store.py
  - backend/tests/test_skills_publish_router.py
  - backend/tests/test_skills_graph_router.py
  - backend/tests/test_skill_relationships.py
  - frontend/src/core/skills/api.ts
  - frontend/src/core/skills/hooks.ts
  - frontend/src/components/workspace/skill-studio-dashboard.tsx
  - frontend/src/components/workspace/skill-studio-dashboard.utils.ts
  - frontend/src/components/workspace/skill-studio-dashboard.utils.test.ts
  - frontend/src/components/workspace/skill-studio-workbench-panel.tsx
  - frontend/src/components/workspace/skill-studio-workbench.utils.ts
  - frontend/src/components/workspace/skill-studio-workbench.utils.test.ts
  - frontend/src/components/workspace/skill-graph.utils.ts
  - frontend/src/components/workspace/skill-graph.utils.test.ts
autonomous: true
requirements:
  - SKILL-03
  - SKILL-04
  - SKILL-05
user_setup: []
must_haves:
  truths:
    - "Revision history and rollback must be centered on the skill asset, not on the thread that created the latest draft."
    - "Graph and dashboard views must gain governance visibility without losing the existing relationship reasoning that already helps routing."
    - "Rollback must restore a real published snapshot, not just flip labels in UI state."
  artifacts:
    - path: "backend/packages/harness/deerflow/domain/submarine/skill_lifecycle.py"
      provides: "Lifecycle registry introduced by 08-01 and enriched by 08-02"
      contains: "binding_targets"
    - path: "backend/app/gateway/routers/skills.py"
      provides: "Current publish and lifecycle APIs"
      contains: "/skills/publish"
    - path: "frontend/src/components/workspace/skill-graph.utils.ts"
      provides: "Current graph overview, focus, and workbench view-model helpers"
      contains: "buildSkillGraphWorkbenchModel"
  key_links:
    - from: "backend/packages/harness/deerflow/domain/submarine/skill_lifecycle.py"
      to: "backend/app/gateway/routers/skills.py"
      via: "Lifecycle revisions and rollback metadata must flow through APIs before the frontend can render them"
      pattern: "rollback_target_id"
    - from: "backend/packages/harness/deerflow/skills/relationships.py"
      to: "frontend/src/components/workspace/skill-graph.utils.ts"
      via: "The graph keeps relationship structure but gains lifecycle badges from registry-backed metadata"
      pattern: "revision_count"
---

# Phase 8 Plan 03: Add Revision History, Rollback, and Post-Publish Discoverability Plus Governance Views

<objective>
Finish Phase 8 by making published skills governable after the first successful publish.

Purpose: the earlier plans create a lifecycle contract and management flow, but the phase is not complete until the user can inspect revisions, roll back to a prior published snapshot, and see governance metadata in the same lifecycle product and graph views. This plan adds real revision storage, rollback APIs, graph metadata enrichment, and frontend surfaces for published-versus-draft comparison.
Output: revision-aware lifecycle storage, rollback behavior, graph and lifecycle APIs with governance metadata, and dashboard/workbench views that expose revisions, rollback targets, and discoverability state.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-skill-studio-lifecycle-and-governance/08-CONTEXT.md
@.planning/phases/08-skill-studio-lifecycle-and-governance/08-RESEARCH.md
@.planning/phases/08-skill-studio-lifecycle-and-governance/08-VALIDATION.md
@.planning/phases/07-workspace-ux-and-navigation-system/07-UI-SPEC.md
@backend/packages/harness/deerflow/domain/submarine/skill_lifecycle.py
@backend/app/gateway/routers/skills.py
@backend/packages/harness/deerflow/skills/relationships.py
@backend/tests/test_skill_lifecycle_store.py
@backend/tests/test_skills_publish_router.py
@backend/tests/test_skills_graph_router.py
@backend/tests/test_skill_relationships.py
@frontend/src/core/skills/api.ts
@frontend/src/core/skills/hooks.ts
@frontend/src/components/workspace/skill-studio-dashboard.tsx
@frontend/src/components/workspace/skill-studio-dashboard.utils.ts
@frontend/src/components/workspace/skill-studio-workbench-panel.tsx
@frontend/src/components/workspace/skill-studio-workbench.utils.ts
@frontend/src/components/workspace/skill-graph.utils.ts
@frontend/src/components/workspace/skill-graph.utils.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist published revisions and implement rollback against stored snapshots</name>
  <files>backend/packages/harness/deerflow/domain/submarine/skill_lifecycle.py, backend/app/gateway/routers/skills.py, backend/tests/test_skill_lifecycle_store.py, backend/tests/test_skills_publish_router.py</files>
  <read_first>backend/packages/harness/deerflow/domain/submarine/skill_lifecycle.py, backend/app/gateway/routers/skills.py, backend/tests/test_skill_lifecycle_store.py, backend/tests/test_skills_publish_router.py, .planning/phases/08-skill-studio-lifecycle-and-governance/08-CONTEXT.md, .planning/phases/08-skill-studio-lifecycle-and-governance/08-RESEARCH.md</read_first>
  <action>Extend the lifecycle registry so every publish appends a revision entry instead of overwriting management metadata in place. Store published snapshots under the exact hidden path `skills/custom/<skill-name>/.revisions/<revision_id>.skill`. Each revision entry must include the exact fields `revision_id`, `published_at`, `archive_path`, `published_path`, `version_note`, `binding_targets`, `enabled`, and `source_thread_id`. When publish succeeds, append the new revision, set `active_revision_id`, and set `rollback_target_id` to the previous active revision when one exists. Add a new rollback route `POST /api/skills/{skill_name}/rollback` that accepts `revision_id`, restores the corresponding snapshot into `skills/custom/<skill-name>`, reapplies the saved enable flag, updates `active_revision_id`, and returns the restored lifecycle summary.</action>
  <verify>`cd backend && uv run pytest tests/test_skill_lifecycle_store.py tests/test_skills_publish_router.py`</verify>
  <acceptance_criteria>
    - backend/packages/harness/deerflow/domain/submarine/skill_lifecycle.py contains `.revisions`
    - backend/packages/harness/deerflow/domain/submarine/skill_lifecycle.py contains `rollback_target_id`
    - backend/app/gateway/routers/skills.py contains `"/skills/{skill_name}/rollback"`
    - backend/app/gateway/routers/skills.py contains `revision_id`
    - backend/tests/test_skills_publish_router.py contains `/api/skills/submarine-result-acceptance/rollback`
    - backend/tests/test_skill_lifecycle_store.py contains `rollback_target_id`
  </acceptance_criteria>
  <done>The lifecycle store now keeps real published revisions and rollback restores an actual saved snapshot instead of only changing metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Expose governance metadata through lifecycle and graph APIs without losing relationship semantics</name>
  <files>backend/app/gateway/routers/skills.py, backend/packages/harness/deerflow/skills/relationships.py, backend/tests/test_skills_graph_router.py, backend/tests/test_skill_relationships.py</files>
  <read_first>backend/app/gateway/routers/skills.py, backend/packages/harness/deerflow/skills/relationships.py, backend/packages/harness/deerflow/domain/submarine/skill_lifecycle.py, backend/tests/test_skills_graph_router.py, backend/tests/test_skill_relationships.py</read_first>
  <action>Extend the lifecycle summary and graph response models so governance metadata becomes first-class API data. `GET /api/skills/lifecycle` and detail responses must expose the exact fields `revision_count`, `active_revision_id`, `rollback_target_id`, `binding_count`, and `last_published_at`. `GET /api/skills/graph` must keep the existing relationship structure from `relationships.py` but merge lifecycle metadata into graph nodes and focused related-skill items using the exact field names `revision_count`, `active_revision_id`, `rollback_target_id`, `binding_count`, and `last_published_at`. Keep `relationship_type`, `score`, and `reason` unchanged so routing semantics do not regress.</action>
  <verify>`cd backend && uv run pytest tests/test_skills_graph_router.py tests/test_skill_relationships.py`</verify>
  <acceptance_criteria>
    - backend/app/gateway/routers/skills.py contains `revision_count`
    - backend/app/gateway/routers/skills.py contains `binding_count`
    - backend/app/gateway/routers/skills.py contains `rollback_target_id`
    - backend/packages/harness/deerflow/skills/relationships.py contains `last_published_at`
    - backend/tests/test_skills_graph_router.py contains `revision_count`
    - backend/tests/test_skill_relationships.py contains `binding_count`
  </acceptance_criteria>
  <done>Lifecycle and graph APIs now expose governance state while preserving the existing relationship reasoning that DeerFlow routing already uses.</done>
</task>

<task type="auto">
  <name>Task 3: Render draft-versus-published, revision history, rollback, and governance badges in the current frontend surfaces</name>
  <files>frontend/src/core/skills/api.ts, frontend/src/core/skills/hooks.ts, frontend/src/components/workspace/skill-studio-dashboard.tsx, frontend/src/components/workspace/skill-studio-dashboard.utils.ts, frontend/src/components/workspace/skill-studio-dashboard.utils.test.ts, frontend/src/components/workspace/skill-studio-workbench-panel.tsx, frontend/src/components/workspace/skill-studio-workbench.utils.ts, frontend/src/components/workspace/skill-studio-workbench.utils.test.ts, frontend/src/components/workspace/skill-graph.utils.ts, frontend/src/components/workspace/skill-graph.utils.test.ts</files>
  <read_first>frontend/src/core/skills/api.ts, frontend/src/core/skills/hooks.ts, frontend/src/components/workspace/skill-studio-dashboard.tsx, frontend/src/components/workspace/skill-studio-workbench-panel.tsx, frontend/src/components/workspace/skill-graph.utils.ts, .planning/phases/07-workspace-ux-and-navigation-system/07-UI-SPEC.md</read_first>
  <action>Update frontend lifecycle helpers and surfaces so governance state is visible inside the current product. Add lifecycle query and rollback client helpers in `api.ts` and `hooks.ts`. Update dashboard entry helpers to expose `revisionCount`, `activeRevisionId`, `rollbackTargetId`, and `bindingCount`. Update `skill-studio-workbench-panel.tsx` so the publish or management section renders a revision list, current `activeRevisionId`, current `rollbackTargetId`, the saved `versionNote`, and a rollback action wired to the rollback mutation. Update `skill-graph.utils.ts` and graph rendering consumers so focused-node helpers surface `revisionCount`, `bindingCount`, and `lastPublishedAt` alongside existing relationship labels and reasons. Keep the outer `Create / Evaluate / Connect` shell and the dedicated graph view intact.</action>
  <verify>`cd frontend && node --test src/components/workspace/skill-graph.utils.test.ts src/components/workspace/skill-studio-dashboard.utils.test.ts src/components/workspace/skill-studio-workbench.utils.test.ts && corepack pnpm typecheck`</verify>
  <acceptance_criteria>
    - frontend/src/core/skills/api.ts contains `rollbackSkillRevision`
    - frontend/src/core/skills/hooks.ts contains `useRollbackSkillRevision`
    - frontend/src/components/workspace/skill-studio-dashboard.utils.ts contains `revisionCount`
    - frontend/src/components/workspace/skill-studio-workbench-panel.tsx contains `activeRevisionId`
    - frontend/src/components/workspace/skill-studio-workbench-panel.tsx contains `rollbackTargetId`
    - frontend/src/components/workspace/skill-graph.utils.ts contains `bindingCount`
  </acceptance_criteria>
  <done>The current dashboard, publish surface, and graph inspector now show real lifecycle governance state without abandoning the approved Phase 7 shell.</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && uv run pytest tests/test_skill_lifecycle_store.py tests/test_skills_publish_router.py`
- [ ] `cd backend && uv run pytest tests/test_skills_graph_router.py tests/test_skill_relationships.py`
- [ ] `cd frontend && node --test src/components/workspace/skill-graph.utils.test.ts src/components/workspace/skill-studio-dashboard.utils.test.ts src/components/workspace/skill-studio-workbench.utils.test.ts`
- [ ] `cd frontend && corepack pnpm typecheck`
- [ ] Publish the same skill twice, then confirm the UI shows at least two revisions and a non-empty rollback target
</verification>

<success_criteria>

- All tasks completed
- `SKILL-04` is backed by real revision history, version notes, active revision state, and rollback targets
- `SKILL-05` is backed by discoverability and governance visibility in both lifecycle and graph surfaces
- `SKILL-03` remains true because publish and enable behavior still operates through the project-local DeerFlow path while gaining revision storage

</success_criteria>

<output>
After completion, create `.planning/phases/08-skill-studio-lifecycle-and-governance/08-03-SUMMARY.md`
</output>
